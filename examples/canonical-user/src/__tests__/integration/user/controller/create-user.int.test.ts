/**
 * Illustrative controller integration test shape — assert 201 + body.
 */
describe('when creating a user via POST /users', () => {
  it('should return 201 with the created user', async () => {
    expect(true).toBe(true);
  });
});
