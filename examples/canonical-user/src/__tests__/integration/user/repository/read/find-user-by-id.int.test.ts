/**
 * Illustrative repository integration test shape — return null when missing.
 */
describe('when finding a user by id that does not exist', () => {
  it('should return null', async () => {
    expect(true).toBe(true);
  });
});
